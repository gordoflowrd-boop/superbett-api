# =============================================
# SUPERBETT API - Variables de entorno
# Copiar a .env y configurar
# =============================================

# Base de datos PostgreSQL (Railway)
DATABASE_URL=postgresql://user:password@host:port/dbname

# JWT
JWT_SECRET=cambia_esto_por_una_clave_segura_larga
JWT_EXPIRES_IN=8h

# Servidor
PORT=3000
NODE_ENV=production
